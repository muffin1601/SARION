/**
 * Homepage demo video config.
 *
 * DEMO_VIDEO_SRC points at a local file served from /public (an <video> tag
 * is used). To use YouTube/Vimeo instead, set DEMO_VIDEO_EMBED_URL to an
 * embed URL (e.g. "https://www.youtube.com/embed/VIDEO_ID") and leave
 * DEMO_VIDEO_SRC null — the embed takes priority when both would apply.
 */
export const DEMO_VIDEO_SRC: string | null = "/sarion-demo.mp4";
export const DEMO_VIDEO_EMBED_URL: string | null = null;

export const DEMO_VIDEO_THUMBNAIL: string = "/screenshots/dashboard-light.webp";
